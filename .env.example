# Copy this file to ".env" and paste your key to enable live weather.
#   cp .env.example .env        (Windows: copy .env.example .env)
# Get a free key at https://openweathermap.org/api  (Current Weather Data).
# A new key can take a little while to activate.
OPENWEATHER_API_KEY=
